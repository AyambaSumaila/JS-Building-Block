
let calculator={
    operand:1,
    operand2:1,

    add(){
        this.result=this.result;s.operand + this.ope
    }
};

calculator.add();
calculator.result(this.result);

function max(first=-Infinity, ...rest){
    let maxValue=first;

    for(let n of rest)
    {
        if(n > maxValue)
        {
            maxValue=n;
        }
    } 
    return maxValue;
}

max(1, 33, 41, 29);

let numbers=[9, 12, 29, -23];
Math.min(...number);


